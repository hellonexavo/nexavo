import { createPageMetadata } from "@/app/lib/seo";
import { faqs } from "../data";
import { ClinicButton } from "../components/ClinicSections";
import { ClinicPage } from "../components/ClinicShell";

export const metadata = createPageMetadata({ title: "Veelgestelde vragen — Modern Dental Care", description: "Antwoorden over afspraken, behandelingen, kosten, nieuwe patiënten, spoed en MDC Junior.", path: "/portfolio/modern-dental-care/faq" });

export default function FaqPage() { const categories = [...new Set(faqs.map((item) => item.category))]; return <ClinicPage><section className="px-5 py-20 md:px-8 lg:px-12 lg:py-28"><div className="mx-auto flex max-w-[1440px] flex-col justify-between gap-8 lg:flex-row lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.22em] text-[#82928d]">Veelgestelde vragen</p><h1 className="mt-5 max-w-4xl text-5xl font-medium leading-[.98] tracking-[-0.06em] sm:text-7xl">Duidelijke antwoorden voor uw <span className="font-serif italic font-normal text-[#9a7849]">bezoek.</span></h1><p className="mt-7 max-w-2xl text-lg leading-8 text-[#687a74]">Van een eerste afspraak tot vragen over behandelingen, kosten en spoed.</p></div><ClinicButton href="/portfolio/modern-dental-care/booking">Afspraak maken <span className="ml-2">↗</span></ClinicButton></div></section><section className="bg-white px-5 py-24 md:px-8 lg:px-12"><div className="mx-auto max-w-[1000px] space-y-14">{categories.map((category) => <section key={category}><h2 className="text-2xl font-medium tracking-[-0.03em]">{category}</h2><div className="mt-5 grid gap-3">{faqs.filter((item) => item.category === category).map((item) => <details key={item.question} className="group rounded-2xl border border-[#173f39]/10 bg-[#f8f8f3] px-5 py-4"><summary className="flex cursor-pointer list-none items-center justify-between gap-4 text-sm font-semibold marker:hidden">{item.question}<span className="text-xl font-normal text-[#6f9084] transition group-open:rotate-45">+</span></summary><p className="max-w-3xl pt-3 text-sm leading-7 text-[#6d7d78]">{item.answer}</p></details>)}</div></section>)}</div></section></ClinicPage>; }
